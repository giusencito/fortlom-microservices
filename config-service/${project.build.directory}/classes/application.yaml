server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: feature/circuit-breaker
          uri: https://github.com/giusencito/fortlom-microservices
          search-paths: config-data

  application:
    name: config-service




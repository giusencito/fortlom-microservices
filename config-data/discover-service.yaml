server:
  port: 8761

eureka:
  instance:
    hostname: discover-service
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://54.147.168.82:${server.port}/eureka/